{
  "name": "my-nft-project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
  "test": "echo \"Error: no test specified\" && exit 1",
  "fullmint": "node uploadImages.js && node generateMetadata.js && node updateMetadata.js && node uploadToPinata.js && npx hardhat run scripts/mint.js --network base"
},
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.5",
    "dotenv": "^16.5.0",
    "ethers": "^6.14.1",
    "form-data": "^4.0.2"
  },
  "devDependencies": {
    "hardhat": "^2.24.0"
  }
}
